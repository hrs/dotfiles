[alias]
  amend   = commit --amend
  br	= branch
  cdiff	= diff --cached
  cm	= commit -m
  ci	= commit -v
  co	= checkout
  dump	= cat-file -p
  hist	= log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
  lg	= log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
  st	= status
  type	= cat-file -t
  unstage = reset HEAD

[core]
  excludesfile = ~/.gitignore

[user]
  name = Harry Schwartz
  email = hrs@ginkgobioworks.com

[color]
  ui = true

[push]
  default = current

[include]
  # for creds
  path = ~/.gitconfig.local
