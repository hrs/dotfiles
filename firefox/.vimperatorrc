" Use `l` and `h` to navigate between tabs.
noremap l gt
noremap h gT

" Scroll faster!
noremap j 5j
noremap k 5k

" Use emacsclient as my external editor (invoked with `C-i` from a text field).
set editor="emacsclient -c"

" Don't you beep at me.
set visualbell

" Use a bigger font for hints. Round the borders, 'cause I'm fancy.
highlight! Hint -append font-family: "Inconsolata"; font-size: 24px; background-color: black; border-radius: 4px;
