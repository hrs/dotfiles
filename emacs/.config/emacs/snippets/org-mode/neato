#+begin_src dot :file `(+diagram-snippet-path)`
  graph {
    graph [layout=neato, overlap=false]
    graph [truecolor=true, bgcolor="#ffffff00"]
    rankdir=LR;

    first [label="First"];
    second [label="Second"];

    first -- second [label="Connection"];
  }
#+end_src
